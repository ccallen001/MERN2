import { LoginForm } from '@/components';

export default function RouteLogin() {
  return (
    <div className="RouteLogin route">
      <h2 className="route-heading">Login</h2>
      <LoginForm />
    </div>
  );
}
